apiVersion: apps/v1
kind: Deployment
metadata:
  name: github-actions-lab

spec:
  replicas: 3

  selector:
    matchLabels:
      app: github-actions-lab

  template:
    metadata:
      labels:
        app: github-actions-lab

    spec:
      containers:
        - name: github-actions-lab
          image: ghcr.io/amarpal-singh5/github-actions-lab:70c6ebf33d3b6dc1efe14d3d56a0b6c3f179706c
          ports:
            - containerPort: 5000

          readinessProbe:
            httpGet:
              path: /
              port: 5000
            initialDelaySeconds: 3
            periodSeconds: 5

          livenessProbe:
            httpGet:
              path: /
              port: 5000
            initialDelaySeconds: 10
            periodSeconds: 10
